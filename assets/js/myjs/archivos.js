 function editarArchivo() {
    alert("Funcionalidad de edición aún no implementada.");
    // Aquí puedes integrar un visor de PDF con capacidades de anotación o redirigir a un formulario de edición
  }

  function eliminarArchivo() {
    if (confirm("¿Estás seguro de que deseas eliminar este archivo?")) {
      alert("Archivo eliminado.");
      // Aquí iría la lógica de eliminación real (backend)
    }
  }
